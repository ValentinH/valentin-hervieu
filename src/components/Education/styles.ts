import styled from '@emotion/styled'
import colors from '../../const/colors'

export const Item = styled('div')({
  marginBottom: 16,
})

export const Date = styled('span')({
  color: colors.primaryColor,
  fontWeight: 'bold',
})

export const Diploma = styled('h3')({
  marginTop: 0,
  marginBottom: 4,
  fontSize: 16,
})
